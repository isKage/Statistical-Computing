## 项目复现

[English](./README.md) / [说明文档](./pdf/hw2.pdf)

首先，配置环境。然后，进入项目根目录。最后，在终端输入命令，即可完整复现本项目所有结果。

- MacOS 系统 / Linux 系统：在 `Terminal` 输入
```BASH
bash main.sh
```
- Windows 系统：在 `cmd` 中输入
```BASH
.\main.bat
```
或者在 `PowerShell` 中输入
```BASH
.\main.ps1
```
或者更简单，直接双击 `main.bat` 脚本。
